Check the questionnaire from [](/data/pfander/questionnaire.html). Translate this into fully text based templates, one for each intervention. Each template should be a fully text-based transcript of the questionnaire with the parts where the participant is answering marked. Try to make it look like a transcript of data published post-experiment on OSF (or somewhere else where experiment data is published). 

Regarding the spots where the user fills out stuff. Clearly mark textually what is the question, what is the valid range of answers and where the response should be:

E.g.
How incompetent or competent are most climate scientists? 0 = very incompetent, 100 = very competent.
Response:

Then for the part after response, come up with your own format to mark both the spot where stuff is generated and exactly which values are valid generations. For context, this is for silicon sampling with a base model LLM and we will have to easily find the spots where answer generation is required.

Please put the text templates in [](/data/pfander/text_templates).